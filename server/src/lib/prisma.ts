/**
 * Future home of the Prisma client singleton once the schema contains models.
 * This placeholder keeps the project structure ready without instantiating
 * Prisma before the data model work begins.
 */
export const prismaPlaceholder = {
  ready: false,
};
